# GraphJudge Environment Variables
#
# Copy this file to .env and customize for local development.
# DO NOT commit .env files with actual secrets!

# =============================================================================
# Green Agent (Evaluator) Configuration
# =============================================================================

# Server settings
GREEN_HOST=0.0.0.0
GREEN_PORT=9009

# AgentCard URL (auto-constructed from host:port if not set)
# GREEN_CARD_URL=https://my-domain.com:9009

# Output path (defaults to output/results.json)
# GREEN_OUTPUT_FILE=output/results.json

# Agent identifier
AGENT_UUID=green-agent

# Purple agent URL for testing (test fixture)
PURPLE_AGENT_URL=http://localhost:9010

# =============================================================================
# Purple Agent (Test Fixture) Configuration
# =============================================================================

# Server settings
PURPLE_HOST=0.0.0.0
PURPLE_PORT=9010

# AgentCard URL (auto-constructed from host:port if not set)
# PURPLE_CARD_URL=http://localhost:9010

# =============================================================================
# LLM Judge Configuration (Optional)
# =============================================================================

# OpenAI-compatible API settings
# If not set, evaluation falls back to rule-based scoring
# AGENTBEATS_LLM_API_KEY=sk-...
# AGENTBEATS_LLM_BASE_URL=https://api.openai.com/v1
# AGENTBEATS_LLM_MODEL=gpt-4o-mini

# =============================================================================
# Platform Deployment (GitHub Secrets)
# =============================================================================

# These are set as GitHub repository secrets for platform runs
# DO NOT add actual values here - this is for documentation only

# GHCR_TOKEN=<GitHub token for container registry>
# API_KEY=<LLM API key for platform runs>
